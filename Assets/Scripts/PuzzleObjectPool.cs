namespace GravityPuzzle
{
    // Kept temporarily so Unity's in-flight compile manifest can retire the old
    // file safely. Progress flights now use FlyingProgressVoxelView's typed pool.
    internal static class PuzzleObjectPool
    {
    }
}
